import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  tasvirDirections: {
    fontSize: 22,
    color: '#4A4A4A',
    fontWeight: '400',
    marginTop: 20,
    textAlign: 'center'
  }
});
